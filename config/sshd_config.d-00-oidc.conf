# Install as /etc/ssh/sshd_config.d/00-oidc.conf.
# OpenSSH generally keeps the first value obtained for global options, so this
# file should sort before vendor configuration that may disable keyboard auth.
KbdInteractiveAuthentication yes

Match User root
    PermitRootLogin yes
    PasswordAuthentication no
    AuthenticationMethods keyboard-interactive:pam
